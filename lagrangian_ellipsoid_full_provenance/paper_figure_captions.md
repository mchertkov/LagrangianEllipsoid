# Draft figure captions

**Figure 1. Löwner--John deform-cloud diagnostic.** Schematic of the pipeline used in the paper. A volume-filled Lagrangian particle cloud is advected by the synthetic incompressible flow, summarized at each time by its minimum-volume enclosing ellipsoid, and converted into the train $(M(t),g(t))$. The intrinsic variables $(v,\sigma,A,\omega,lpha)$ expose the aspect-ratio source--sink balance.

**Figure 2. Empirical MEE shape statistics.** The log-aspect-ratio $\sigma$ is an $O(1)$ fluctuating state variable. Panel (a) shows $\langle\sigma\mid r
angle$ with seed-level uncertainty. Panels (b,c) show that the conditional distributions are broad and retain visible scale dependence.

**Figure 3. Coarse-grained gradient statistics.** Panel (a) shows the scale dependence of $\langle |M|_{
m red}^2\mid r
angle$. Panel (b) shows the corrected tensor-level strain--vorticity ratio $\chi(r)$, with $\chi=1$ corresponding to ordinary two-dimensional incompressible balance. Panel (c) summarizes mild component non-Gaussianity.

**Figure 4. Empirical source--sink mechanism.** The aspect-ratio budget $\dot\sigma=2A\coslpha+R_\sigma$ decomposes the dynamics into an aligned-strain source and a Löwner--John residual. The positive source and negative residual nearly balance over much of the inertial range, explaining the observed $O(1)$ aspect ratio.

**Figure 5. Null-model failure.** A marginal-gradient stochastic model fails because it does not preserve the empirical alignment between the strain and ellipsoid axis. Without this alignment the source $2A\coslpha$ vanishes, and the model cannot sustain the empirical aspect ratio.

**Figure 6. Physics-informed closure validation.** The final interpretable closure combines scale-dependent stochastic drivers for $A$ and $\omega$, a stationary von--Mises alignment bias for $lpha$, and an affine residual $R_\sigma=a_0(r)+a_1(r)\sigma$. This VM1 closure substantially improves the aspect-ratio prediction relative to the marginal-OU null model and the more restrictive VM0 residual.
